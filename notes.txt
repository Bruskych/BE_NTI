/*

--- DOCKER ---
0. Скопировать и переименовать файл .env.example в .env
0. php artisan key:generate
1. cd C:\Users\user_name\PhpstormProjects\NTI_project\NTI_backend       -> Project folder where you have Dockerfile
2. docker compose up -d --build                                         -> Build and Start (Connect docker)
3. docker compose exec app php artisan migrate --seed                   -> migrate / seed
4. docker compose ps                                                    -> Check / Test docker

5. docker compose down
6. docker compose up -d --force-recreate                                -> Build and Start (With .env)
7. docker compose logs frontend                                         -> Logs Frontend
8. docker compose restart webserver                                     -> Restart WEB

--- GIT ---
git reset --soft HEAD~1             -> delete last commit

--- START ---
C:\xampp\php\php.exe artisan serve
php artisan serve

--- TINKER ---
php artisan tinker
use App\Models\Note;
Note::all();

--- API ---
php artisan install:api



--- DATABASE ---
php artisan migrate                         -> load
php artisan db:seed                         -> start add seeder
php artisan db:seed --class=NameSeeder      -> test seeder
php artisan migrate:rollback                -> back 1 step
php artisan migrate:fresh                   -> delete
php artisan migrate:fresh --seed            -> delete+add

    === FACTORIES ===
    php artisan make:factory NameFactory --model=Name

    === MIGRATIONS ===
    php artisan make:migration create_NAME_table

    === SEEDER ===
    php artisan make:seeder NameSeeder



--- IDE HELPER ---

    === GENERATE IDE HELPER FOR FACADES ===
    php artisan ide-helper:generate

    === GENERATE IDE HELPER ===
    php artisan ide-helper:meta

    === GENERATE IDE HELPER FOR MODELS ===
    php artisan ide-helper:models --nowrite

    === DELETE IDE HELPER FOR MODELS ===
    php artisan ide-helper:models --reset



--- CONTROLLER ---
php artisan make:controller NameController --api

--- MODEL ---
php artisan make:model Name -m

--- POLICIES ---
php artisan make:policy NotePolicy --model=Note

--- LISTENER ---
php artisan make:listener NAME --event=Registered

--- MAIL ---
php artisan make:mail WelcomeMail

--- ARTISAN COMMAND ---
php artisan make:command SendPremiumExpiryEmails

*/
